import React from 'react'
import { Realestate } from '../components/components'

const Nekretnine = ({ scrollToTop }) => {
  return (
    <>
      <Realestate scrollToTop={scrollToTop} />
    </>
  )
}

export default Nekretnine